instructions to run the bot:

Connect the bluetooth module to the bot.

Burn the code into the bot using AVR bootloader.

Just keep the bot on the grid with whitelines, on a junction(the coordinates 0, 0)

Bot bot will run based on the signals received from the phone.
